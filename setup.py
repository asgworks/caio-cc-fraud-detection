from setuptools import setup, find_packages

setup(
    name="caio-cc-fraud-detection",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Add dependencies from requirements.txt
    ],
)
